@model IEnumerable<WebbanBALO.Areas.Admin.Models.Framework.Sale>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2>Sản Phẩm Sale</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tenspsale)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Giasale)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Anhsp)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tenspsale)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Giasale)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Anhsp)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Masale }) |
            @Html.ActionLink("Details", "Details", new { id=item.Masale }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Masale })
        </td>
    </tr>
}

</table>
